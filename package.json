{
  "name": "restaurant-order",
  "version": "1.0.0",
  "private": true,
  "description": "Restaurant waiter and kitchen ordering app",
  "scripts": {
    "check": "node --check server.js && node --check public/app.js && node --check lib/api.js && node --check lib/db.js && node --check lib/repository.js && node --check lib/config.js",
    "migrate:json": "node scripts/migrate-json-to-postgres.js",
    "start": "node server.js"
  },
  "dependencies": {
    "pg": "8.11.5",
    "web-push": "3.6.7"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
